name: CI
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  quality:
    name: Lint, types, tests, and production build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SEARCH_BACKEND: demo
      OPENAI_API_KEY: ''
      NEXT_TELEMETRY_DISABLED: '1'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
  opensearch:
    name: Real OpenSearch integration
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SEARCH_BACKEND: opensearch
      OPENSEARCH_URL: http://127.0.0.1:9200
      OPENSEARCH_INDEX: talentlens-ci
      OPENAI_API_KEY: ''
      RUN_OPENSEARCH_TESTS: '1'
    services:
      search:
        image: opensearchproject/opensearch:2.19.3
        env:
          discovery.type: single-node
          DISABLE_INSTALL_DEMO_CONFIG: 'true'
          DISABLE_SECURITY_PLUGIN: 'true'
          OPENSEARCH_JAVA_OPTS: -Xms512m -Xmx512m
        ports:
          - 9200:9200
        options: >-
          --health-cmd "curl -fsS http://localhost:9200/_cluster/health || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run seed
      - run: npm test
      - name: Run synthetic sanity benchmark (not human relevance evaluation)
        run: npm run evaluate
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: opensearch-sanity-benchmark
          path: data/evaluation-opensearch-keyword.json
          retention-days: 14
